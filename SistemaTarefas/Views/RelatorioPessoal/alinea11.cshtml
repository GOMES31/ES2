@using SistemaTarefas.Context
@using SistemaTarefas.Entities
@addTagHelper*, Microsoft.AspNetCore.Mvc.TagHelpers
 
@model List<Tarefa>
@{
    ViewData["Title"] = "alinea11";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
 
<!DOCTYPE html>
 
<html>
<head>
    <meta name="viewport" content="width=device-width"/>
    <title>Index</title>
</head>
<body>
<form method="post" asp-controller="RelatorioPessoal" asp-action="alinea11">
    <span>Id do Utilizador:</span><input type="number" name="id"/>
    <span>Data a ver (pode ser usado qualquer dia dentro do mes pretendido):</span><input type="date" name="data"/>
    <input type="submit" value="Search"/>
    <br/>
    <br/>
    <table class="table">
        <tr>
            <th>ID Tarefa</th>
            <th>Data de Início</th>
            <th>Data de Fim</th>
            <th>Descrição</th>
            <th>Estado</th>
            <th>ID Projeto</th>
            <th>Preco por Hora</th>
            <th>Nº Horas</th>
            <th>Dinheiro Ganho</th>
        </tr>
        @foreach (Tarefa customer in Model)
        {
            @if (@customer.estado == "finalizado")
            {
                <tr>
                    <td>
                        @customer.id_tarefa
                    </td>
                    <td>
                        @customer.hora_inicio
                    </td>
                    <td>
                        @customer.hora_fim
                    </td>
                    <td>
                        @customer.descricao
                    </td>
                    <td>
                        @customer.estado
                    </td>
                    <td>
                        @customer.id_projeto
                    </td>
                    <td>
                        @customer.precohora
                    </td>
                    <td>
                        @{
                            TimeSpan teste = (DateTime)customer.hora_fim - customer.hora_inicio;
                        }
                        @(Math.Round(teste.TotalHours, 0))
                        
                    </td>
                    <td>
                        @(Math.Round(teste.TotalHours, 0)*24)
                    </td>
                    
                </tr>
            }

        }
    </table>
</form>
</body>
</html>